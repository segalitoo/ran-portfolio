# Video masters. The Zoomi ProRes 4444 is 361MB.
mov/*.mov
*.prores.mov

# Tooling installed into the repo, not part of the site.
.claude/
.github/agents/
.github/hooks/
.github/skills/
.impeccable/

# Design explorations. Kept on disk and in history at fd8afab,
# but not deployed: GitHub Pages serves whatever is committed.
_archive/
